# Product Overview

PDF Downloader for Google Sheets - a Google Apps Script that automates batch downloading of PDFs from URLs listed in a spreadsheet. The script reads URLs from a specified column, extracts embedded PDF links, downloads the files, saves them to a Google Shared Drive folder, and writes shareable links back to the spreadsheet. Designed for teams that need to centralize PDF storage and maintain easy access through spreadsheet references.
